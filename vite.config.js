import { defineConfig } from "vite";

export default defineConfig({
  // Relative paths make the generated site work at
  // https://USERNAME.github.io/REPOSITORY-NAME/
  // as well as at a custom domain or local preview.
  base: "./",
});
